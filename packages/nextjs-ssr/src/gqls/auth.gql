mutation login($account: String!, $password: String!) {
  login (account: $account, password: $password) {
    content {
      token
    }
    code
  }
}

query info {
  userInfo {
    content {
      username
      email
    }
    code
  }
}

mutation logout {
  logout {
    code
  }
}